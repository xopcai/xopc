# Documentation screenshots

Place approved user-documentation screenshots in this directory. Filenames and capture requirements are defined in `docs/design/documentation-screenshot-plan.md`.

Before adding an image, remove API keys, tokens, account identifiers, private messages, and personal paths. User guides refer to these assets as `/screenshots/<filename>` only after the file exists.
