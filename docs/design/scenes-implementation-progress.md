# 场景实施记录

更新：2026-09-21。当前实现以[通用场景任务跟进方案](./scene-task-follow-up.md)为准。下方 Slack 开发首版保留为历史验收记录，其专用 API、Agent 和 Docker 前置要求已被通用链路替代；不把隔离测试等同于真实账号上线。

## 2026-09-21 通用任务跟进重构（当前、未发布）

### 已实现

- 移除 `DevelopmentSceneService` 和直接创建 pi Agent 的专用执行器，替换为来源注册表、`TaskFollowUpService`、已有 Task／TaskRunDispatcher、`createDelegateChildHandle`／embedded harness 与 SQLite 会话。业务步骤由目标和自然语言指令决定，不按 Slack、编码或文档建立业务分支。
- 同一入口选择无文件资源、任务产物目录或 worktree；文件读写与命令验证分别授权。文件修改不要求 Docker，文档产物不要求项目或 Git。可选验证工具保留固定镜像隔离策略，不自动在宿主机执行仓库代码。
- 来源观察、交付、处理版本独立记录，未验证不会被标成测试通过。模型明确返回是否立即继续；不能根据非空“剩余工作”推断循环。每来源版本最多三轮，每任务每日十轮，重复来源不产生新运行。回答问题、来源变化继续使用原 Task、会话和资源。
- 通用来源 API、创建／详情、暂停后修改指令与能力授权已接通。界面采用渐进披露，将资源、写入授权、可选验证分开；沿用现有设计系统、固定尺寸滚动弹窗、窄屏布局和组件。
- v191 追加迁移保留 v190 任务、来源记录、分支、资源及回执，并暂停旧实例等待确认；不重建任务，不自动扩大权限。旧 `/api/scenes/development` 路由和专用 UI 已移除。

### 验证事实

- `pnpm run build` 完整通过，根类型检查、变更代码 ESLint 通过。完整回归为 **1,286 个测试文件通过、3 个跳过；7,393 项通过、12 项跳过，1 项失败**。唯一失败是已有 `src/computer/evaluation-harness.ts` 直接导入 `node:child_process` 导致的进程边界检查；当前 HEAD 已包含该导入，本轮未修改这两个文件，也未放宽断言。
- 最后补充迁移后权限排序幂等、非阻塞剩余工作不循环、显式继续与三轮上限后，任务跟进／执行器／迁移聚焦回归 **3 个文件、33 项全部通过**。另有既有场景、Gateway 来源／lazy 路由、委托 harness／Workflow、任务与通知回归；覆盖来源去重、暂停／撤权、原任务与资源复用、用户回答、恢复、通用 mail fixture／无资源任务、无 Git 文档、无 Docker 文件编辑，以及实际 embedded harness 的受限工具、结构化结果与 SQLite 会话复用。
- `scripts/task-follow-up-smoke.mts`：真实 Chrome、鉴权 lazy Gateway、SQLite、TaskRunDispatcher、临时 Git／worktree 的端到端通过。来源与模型明确使用 fixture；验证更新复用、暂停后修改、旧路由 404、未鉴权 401，以及桌面／390px 窄屏、明暗主题和无横向溢出。
- `scripts/scenes-gateway-smoke.mts`：完整构建产物的真实 Gateway 启动、鉴权路由、新入口、既有场景执行、定时运行、重启幂等、关闭开关返回 503、旧路由移除与数据库外键检查通过。
- `scripts/task-follow-up-live-smoke.mts`：读取已授权测试 Slack 线程，通过现有真实 BYOK 模型运行时，分别完成 **worktree 代码修改** 和 **无项目／无 Git 的 Markdown 文档整理**。两次均删除测试配置中的 Docker 隔离项、未授予命令执行权限，使用同一协调器和 Agent harness。两次 `processedRevision=1`、`needsUser=false`、验证状态均如实为 `unverified`；各读取来源五次，分别持久化 12／10 条会话消息，无变化轮询不新增运行。测试结束已暂停，未修改主配置或迁移主数据库。
- 真实模型验收保存在本机临时目录 `xopc-task-follow-up-live-PV4V4g`（代码）和 `xopc-task-follow-up-live-K7WRQY`（文档）。另在完整审阅合成代码后执行独立开发验收：17 项生成测试及独立非法输入／负数／小数行为断言通过；这不是授予后台 Scene 命令权限，其原回执仍保持“未验证”。原始仓库没有额外改动或提交，未推送、部署或发送 Slack 消息。

### 当前边界

主 Gateway 的 Scene 开关仍为关闭，当前运行进程没有替换；本轮为源码实现和隔离验收，尚未安装／发布。仍需显式启用并使用新构建启动才能在主实例使用。真实撤权／恢复、系统通知送达和持续个人试用尚不宣称完成。当前来源入口只接手明确 Slack thread；全频道事项发现、更多生产来源、可安装 Skill／Extension 能力包及 Workflow 引用后续推进，不纳入本轮已交付范围。

## 2026-09-21 Slack 开发首版（历史、已被上方重构替代）

用户确认进入开发后，完成明确 thread → Scene → Task／TaskRun → 内部 BYOK Agent → 独立 worktree／任务分支 → 验证回执的代码链路；已有分支可只读盘点并人工关联。没有新建第二套任务状态、DAG 编辑器或外部 Codex 调度平台。

- Slack 接入复用 Composio 当前账号与读取策略，包含 workspace／thread 身份、完整分页、编辑／删除、撤权复查、重复提交和失败退避；附件明确标记未读取。
- 新讨论回到同一任务和分支；保留人工调整的契约，过时执行不能认证新版本；用户回答后自动继续。只读调查可显式授权升级编码，仍用原任务与资源。
- 文件工具限定到独立工作区，已有文件读后校验再改；测试仅通过固定镜像 Docker，断网、凭据遮蔽，无宿主回退。测试失败在预算内自动修复一次。
- 资源操作身份、单写者栅栏及来源修订持久化。重启／终止未知时保留现场等待核对，暂停、撤权和全局检查暂停在检查点阻止继续。
- 验证绑定源码指纹并在交付前重读来源；通过约定命令进入审阅，不宣称语义完全正确、已合并或已上线。正常进度静默，阻塞复用 Task 通知并遵守静音。
- Web 新建／详情／来源链接／验证证据／旧分支关联已接通；渐进展示编码授权和容器设置。完整边界、预算、文件映射与 API 见[首版实现说明](./slack-development-implementation.md)。

### 本地验证

- 最终全量回归：1,281 个测试文件通过、3 个文件跳过；7,377 项通过、12 项跳过。最终轮询筛选与集成测试超时调整另复验开发服务 19 项通过；未改动功能断言。
- 真实 pi Agent 工具循环＋本地模型 SSE fixture，验证受限读取、写入、验证工具及凭据／元数据隔离。
- 真实 Chrome＋鉴权 lazy Gateway 路由＋TaskRunDispatcher＋SQLite＋临时 Git 仓库：创建、连续来源修订、原任务／工作区复用、原仓库不变、分支人工关联、暂停，以及调查后授权编码。Slack、模型业务结果及 Docker 验证使用明确 fixture，不冒充外部端到端验收。
- 桌面、375px 深色窄屏、横屏与减少动效设置检查通过；无页面运行错误或横向溢出。
- Node／类型声明／Web 完整构建通过；构建产物正式 Gateway 启动、鉴权、旧场景 Agent 执行、定时运行、重启幂等、关闭开关与数据库外键完整性通过。变更代码 ESLint、类型检查、依赖边界与文档检查通过。现有 Vite 大 chunk／动态导入警告未作为本次范围扩张处理。
- 升级测试改用真实 v188／v189 基线，验证 v190 保留原场景；未连接或迁移用户的真实数据库。双数据库迁移集成测试使用 30 秒超时，避免与全量并行 fsync 竞争造成原 5 秒门槛误报。

### 真实环境验收

后续用户完成环境准备后，已确认 Docker 29.4.0 在线，现有两个 Slack 账号通过正式 Gateway 的 `SLACK_TEST_AUTH` 真实调用，返回有效且不同的 workspace 身份；未扫描频道或读取未指定的业务消息。

新增 `scripts/development-docker-smoke.mts`，通过产品真实 `verifyDevelopmentWorkspace` 调用已安装的 `node@sha256:83f487e0a63425e5b4d146fb5e5be574bcbe1b7b843d3ebafdd95eaf7767a7e5` 镜像。合成项目的测试通过／测试失败不认证、源码只读、凭据遮蔽、网络关闭、临时输出可写、运行中取消与容器清理共七项真实 Docker 检查通过。未拉取镜像，未修改用户仓库或全局配置。脚本不在现有 ESLint 配置覆盖范围，验收依据为实际运行结果。

用户随后明确授权直接操作测试 Slack。已通过登录中的浏览器，在 `micjoyce` 工作区创建仅当前用户的私有频道 `xopc-scene-e2e-20260921`，发布了[合成计算器需求线程](https://micjoyce.slack.com/archives/C0C2UM8M1AT/p1789983122043189)。没有邀请其他人、修改已有消息或接管业务仓库。

调用正式 Gateway 的 `SLACK_FETCH_MESSAGE_THREAD_FROM_A_CONVERSATION` 读取该线程时，服务返回 HTTP 400：`Daily managed connector execution quota exceeded`。这是托管连接器日额度阻塞，不是 Docker 未准备或用户未提供线程；没有修改额度、计费、全局权限或绕过限制。Codex 自身 Slack 插件还提示登录过期，但浏览器可用，无须将插件重新授权作为当前产品验收的先决条件。

补充 `scripts/development-agent-live-smoke.mts`：使用现有 `xopc-cloud/deepseek-v4-flash`、正式 `DevelopmentAgent` 与正式 Docker 验证器，在合成 Git 仓库的独立 worktree 中执行。第一次真实运行发现模型以普通文字而非 JSON 结束，导致代码已修改但回执解析失败；已增加一次禁用所有工具的回执格式修复，共享原模型预算，仍严格校验结构、不将无法解析的结果当成功。新增四项回归覆盖格式修复、围栏 JSON、再次无效及预算限制。相关 35 项测试、变更文件 ESLint、根类型检查通过。

修复后第一阶段真实模型完成加法修复及测试（6 次模型调用／6 次工具调用）；第二阶段在同一 worktree／分支追加仅接受有限数字的输入校验（8 次模型调用／9 次工具调用），最终 8 项仓库测试以及 Agent 无法修改的独立行为断言通过。第二阶段曾遇到文件写入不完整导致编译失败，Agent 根据验证结果重新修改后通过。原仓库保持干净且未产生额外提交，无遗留运行中验证容器。代码及 `phase-1.json`／`phase-2.json` 保留在本机临时目录 `xopc-development-live-model-J2t8pq` 供复查。此脚本使用显式合成证据，不通过 Slack 连接器；不能替代 Slack → Task → 编码的真实端到端验收，也不证明所有语义要求已满足。模型回执中的其他不确定性保留供审阅。

#### BYOK 连接恢复后的真实闭环复验

用户处理连接问题后，发现其新增的 BYOK Composio Slack 账号与原托管账号属于同一 `micjoyce` workspace。旧账号调用仍返回托管额度耗尽；新账号真实 thread 读取 HTTP 200。没有把切换默认 backend 当作旧授权自动迁移，没有修改用户连接配置、权限或额度。

新增 `scripts/development-slack-live-smoke.mts`，使用正式 Slack 来源适配器、Scene 鉴权 lazy 路由、TaskRunDispatcher、Task 会话仓库、原生模型执行器、worktree 管理器及 Docker 验证器。场景与任务保存在隔离 SQLite，Slack 只读请求转发到用户正在运行的正式 Gateway，沿用该 Gateway 的实际账号权限；模型与 Docker 均为真实调用，没有注入消息／模型结果／验证结果替身。真实数据库仅用于读取选定连接的元数据，不复制凭据。追加消息由验收操作通过 Slack 浏览器发送，不是给开发 Agent 新增 Slack 写入工具。

- 第一阶段从真实 Slack 原始需求完成加法修复，来源版本 1 应用成功。
- 通过浏览器在原线程发送输入校验追加要求；重新启动验收进程，正常调用产品轮询入口读取新回复，没有伪造来源内容或强制修改游标。
- 第二阶段仍为 Task `c1a1e4d2-d187-43c6-b4af-bafaa09b8232`、同一 worktree 和分支 `xopc/task-c1a1e4d2-d187-43c6-b4af-bafaa09b8232`。observed／applied 均为 2；最终 19 项 `node --test` 测试及 Agent 无法修改的独立容器断言通过。
- Task 保持 `review`，回执 `needsUser=false`、`remainingWork=[]`；分支盘点可反查任务。原始仓库保持干净，没有额外提交、推送或部署。外键检查通过，无遗留运行中验证容器。
- 完成后通过鉴权 API 暂停测试 Scene。报告、SQLite 与代码保留在本机临时目录 `xopc-slack-live-K1zEYD`，包含 `phase-1.json`／`phase-2.json`。

本轮发现并处理两点：模型把“等待未来讨论／将来重跑测试”填入当前 `remainingWork`，造成不必要的人工等待；现明确仅列当前来源版本尚未完成的要求，不猜测未来需求，非阻塞限制写入 summary，保持原验证门槛。另一个是验收脚本重启时错误地新建任务会话，触发单工作区绑定保护；脚本改为复用正式 Task 会话仓库，并保留该次失败记录，未删除失败历史或解除工作区保护。最终数据库保留两个成功运行、一个脚本会话绑定失败运行。35 项相关回归、变更源码 ESLint、根类型检查通过。

当前用户主运行配置仍关闭 Scene，未向其主数据库添加开发 Scene。本轮证明真实 Slack → Task → 内部 Agent → Docker → 新回复继续原任务的组件集成闭环，不等同于已安装／发布或持续在线运行。仍未完成：真实撤权／恢复、业务仓库依赖环境、系统通知送达及连续个人试用。尚未发布、推送、创建 PR 或操作已有业务分支。频道发现、外部 Codex 和增强 Extension 仍按已确认首版范围后置。

## 2026-09-21 开发前产品方向确认（历史记录）

下一迭代以用户自己的 Slack 消息跟进、持续排查／编码和分支关联为首个闭环。已更新产品、技术与 S0—S5 计划；不以客户交付人群访谈作为启动前提，不先合并 Scene／Extension。

- 待实现：Slack thread 接手与持续同步、Task 工作说明修订、向原执行任务投递及应用确认、工作区／分支绑定、真实编码与验证回执。
- 默认架构路线：内部 BYOK Agent；外部 Codex 可选，先完成一种可验证执行器，不能把 provider 支持当作外部任务集成完成。
- 待验收：真实 Slack／仓库／执行器、讨论变更、断线恢复、分支反查及用户实际减负。
- 本轮未运行功能测试、未读取真实 Slack、未创建开发分支或派发 Agent，也未发布。
- 下文已有实现及真实环境门禁保持原结论；本次方向调整不授权再次清理场景数据或重做旧运行时迁移。

## 2026-09-20 只读场景交付记录

## 已确认的边界

旧 Proactive／助理 Heartbeat 未正式使用，不保留实验历史。删除逐表转换、归属映射、对账、快照、跨配置 journal 和历史导入界面。普通 SQLite v188 事务负责删除旧实验表和相关通知、安装当前结构；失败回滚、重复启动不清空新场景。废弃 `gateway.heartbeat` 配置（含错误类型）在解析时忽略、保存时移除。

不清空聊天、项目、账号、独立自动化或整库，不扫描／导入／删除用户工作区文件。其他已发布功能的升级机制保持原有职责。

首发：单 Gateway／SQLite、本地主体、Web 管理、Gmail 指定线程和用户提供资料的家庭安排。只准备建议／草稿，不发送邮件、不执行跨系统写入。后台提醒先使用 Web Push；移动端不接收无法处理的新场景推送，旧判断入口已移除。

## 本轮实现

- 正式 `GatewayService` 构造新宿主，提供鉴权 API 和 realtime；模型初始化完成后启动，退出和启动失败时停止执行／通知。
- 删除旧 Proactive、助理 Heartbeat worker、事件发布者、API、Web 设置与跟进入口、旧项目监控；项目、任务、自动化及连接器学习保持独立。
- `xopc_use` scene 命令与 Web 使用同一应用服务，遵守幂等、权限与修订控制。
- 新 Gmail picker 显式搜索账号，保存最小账号／线程身份，不依赖知识同步。执行时读取当前连接，前后复查授权与线程变化；要求每次确认或 Agent 限定的账号不进入首发候选。
- 模型配置／凭据预检；目标编辑、资料、时间、手动检查、暂停、恢复、结束、成果复制、反馈和诊断。
- 鉴权偏好及 presence API／UI：暂停检查、静音、时区、免打扰、摘要、每日上限、浏览器提醒开关。
- Web Push 订阅及现有通知账本接线。发送前复查权限、当前成果、免打扰和查看租约；推送只携带通用提醒与应用内路径。`accepted` 表示推送服务接受，不声称设备已收到。未知发送结果不自动重发。
- 邮件逐项 5 秒超时、20 秒批次时间预算、逐项进度游标、账号退避；慢来源不会反复把后续工作卡在同一页。
- 诊断包含排队数、等待时长、最近／下次检查、模型 token 和估算费用、Connector 请求计数、投递状态。
- 小批清理 90 天以前的终态运行上下文／用量记录、过期 presence／订阅以及未被场景引用的 30 天旧搜索元数据。保留成果、反馈、请求身份和通知去重记录，避免清理后重放。

## 验证证据

- 全量运行：1,262 个测试文件、7,265 项通过，12 项跳过；后续增量复验 29 个文件、296 项通过，覆盖留存、连接器用量、首发渠道及启动清理。
- 根／Web／移动端类型检查、变更文件 ESLint、dependency-cruiser 已通过。完整 Node／声明／Web 构建、Electron 运行产物构建和 macOS ARM64 未签名目录打包通过；打包运行依赖校验通过，未发布或安装到用户环境。
- Chrome 隔离流程：创建响应丢失重试、资料冲突、时间设置、检查、反馈、成果收件箱、邮件搜索／草稿、结束跟进和窄屏布局。
- `scripts/scenes-gateway-smoke.mts` 使用真正的 Node 构建产物及正式 Gateway 启动链：鉴权、家庭场景、真实 Agent 执行器＋本地模型 HTTP 服务、成果、重启幂等、定时运行、暂停、旧接口 404、外键完整性。
- 新装、v165／175／177 旧实验数据、无效旧 JSON／配置、失败回滚重试和升级后数据保留均有覆盖。另以最新 Node 构建产物验证新装、v175 损坏旧数据／配置及重复启动，服务正常就绪、外键完整、新场景数据保留。
- 通知模拟传输验证：正常接受、撤权取消、查看抑制、重启不重发；真实 Gmail 和真实设备接收尚未验证。

## 仍需真实环境完成的上线门禁

1. 指定测试 Gmail 账号、线程和模型：到期无回复、有回复静默、新回复撤回旧草稿、撤权及重新授权。
2. HTTPS 或 localhost 浏览器授予推送权限：页面关闭、系统后台／前台、拒绝权限、点击定位、服务重启后无重复。Electron 仅承诺管理与成果查看，不能把 Web Push 模拟验收当作 Electron 系统通知验收。
3. 选定首发真实负载，持续试点至少 7 天，记录成本、错误、误报／漏报、截止延迟和打扰反馈。单运行队列的容量必须实测，不承诺无限场景数。
4. 最终发布包的设备验收、版本发布与发布后观察。未发布到 npm／商店，未替换真实用户环境。

不得将以上本地验证描述为“已经生产上线”或“真实提醒已经送达”。

使用说明见[使用场景](../scenes.md)。用户已确认真实账号／浏览器环境稍后提供；本轮先完成代码与离线验收，不以本地替身覆盖上述真实环境门禁。

## 2026-09-20 真实环境首轮验收

用户授权使用现有 xopc Gmail 连接。通过当前本机 Gateway 的鉴权 API 验证账号发现与真实邮件搜索（26 个去重线程），选择课程自动提醒进行到期检查。真实 Gmail 线程读取和已配置的模型执行通过，结果为 `no_relevant_change`，未生成无用草稿。

使用明确标注为虚构的家庭安排测试真实模型成果生成：成功识别周六公园与午餐的时间冲突，生成中文调整选项，成果出现在收件箱。两个验收场景均已暂停，便于用户查看，不继续后台检查。没有发送邮件、创建 Gmail 草稿或修改邮件标签。

真实验收发现并修复两处集成问题：

- Composio 成功响应可能只有 `data` 和 `error: null`，不提供 `successful: true`。搜索及线程读取接受该格式，仍拒绝显式失败、错误和缺失消息数据。
- 执行器提示词曾允许模板未支持的 `observation` 类型。现按模板列出允许类型，并对不合格模型输出记录 `invalid_model_result`，页面展示可操作的诊断。

修复后相关 46 项测试通过。真实新回复、撤权／重新授权、浏览器系统通知和持续试点仍未完成；此次通过不能替代这些门禁。此前 Electron 未签名包早于本轮修复，发布前需重新打包。

## 发布评审修复

五项评审问题已落实：持久化邮件来源健康状态，暂缓故障期间的旧成果提醒；相同上下文的手动检查复用原成果；创建后显示设置清单；区分截止时间、排期与重试；成果提供生成时间和可读来源入口。v189 仅新增运行状态表，升级保留 v188 现有场景。

29 个文件、307 项回归通过，另通过新增升级保留场景测试。Web 构建、浏览器交互及隔离 Gateway 启动、执行、重启、数据库完整性验收通过。详见[发布评审](./scenes-production-readiness-review.md)。真实系统推送和长期试点仍作为发布前验收，不以离线回归替代。
