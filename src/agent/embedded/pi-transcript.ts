/**
 * pi-coding-agent transcript file API (OpenClaw-aligned naming).
 * Do not confuse with {@link SessionIndex} in `src/session/manager.ts`.
 */
export {
  SessionManager as PiTranscriptManager,
  type SessionManager,
  CURRENT_SESSION_VERSION,
} from '@earendil-works/pi-coding-agent';
