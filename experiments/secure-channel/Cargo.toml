[package]
name = "xopc-secure-channel-probe"
version = "0.0.0"
edition = "2024"
publish = false

[dev-dependencies]
snow = { version = "=0.10.0", default-features = false, features = ["std", "use-curve25519", "use-chacha20poly1305", "use-blake2", "use-getrandom"] }

serde_json = "=1.0.145"
hex = "=0.4.3"

[workspace]
