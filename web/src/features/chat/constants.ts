/** Max files per chat message (keep in sync with gateway `chat-limits`). */
export const MAX_CHAT_ATTACHMENTS = 10;

/** Max bytes per file before attach (keep in sync with gateway `MAX_WEBCHAT_ATTACHMENT_FILE_BYTES`). */
export const MAX_WEBCHAT_ATTACHMENT_FILE_BYTES = 32 * 1024 * 1024;
