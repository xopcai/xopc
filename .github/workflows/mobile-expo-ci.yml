name: Mobile Expo CI

on:
  workflow_dispatch:
  push:
    branches: [main, master]
    paths:
      - "apps/mobile-expo/**"
      - "packages/gateway-sse-client/**"
      - "scripts/apps/mobile-expo/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/mobile-expo-ci.yml"
  pull_request:
    branches: [main, master]
    paths:
      - "apps/mobile-expo/**"
      - "packages/gateway-sse-client/**"
      - "scripts/apps/mobile-expo/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/mobile-expo-ci.yml"

concurrency:
  group: mobile-expo-ci-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint-typecheck-test:
    name: Lint, typecheck & tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Install pnpm
        uses: pnpm/action-setup@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Lint mobile Expo app
        run: pnpm run mobile:lint

      - name: Typecheck mobile Expo app
        run: pnpm run mobile:typecheck

      - name: Test mobile Expo app
        run: pnpm run mobile:test

      - name: Test agent stream client
        run: pnpm run mobile:test:stream

      - name: Validate iOS manual signing prebuild
        env:
          SIGNING_STYLE: manual
          APPLE_TEAM_ID: TESTTEAM
          IOS_PROVISIONING_PROFILE_MAIN: main-profile
          IOS_PROVISIONING_PROFILE_SHARE: share-profile
          IOS_PROVISIONING_PROFILE_WIDGET: widget-profile
          EXPO_NO_TELEMETRY: "1"
          XOPC_IOS_USE_COCOAPODS_MIRROR: "0"
        run: pnpm -C apps/mobile-expo exec expo prebuild --platform ios --no-install --clean
